{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "Open guidebooks, backpacks, handhelds and more with a single click in inventory",
  "authors": [
    "Zank"
  ],
  "contact": {
  },
  "license": "LGPL-3.0",
  "icon": "assets/${id}/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "zank.mods.open_in_inventory.OpenInInventoryFabric"
    ]
  },
  "mixins": [
    "template-common.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.15",
    "minecraft": "${minecraft}"
  }
}